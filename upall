#!/usr/bin/env ruby

puts "Updating Ruby gems"
`gem update`

puts "Updating Homebrew recipes"
`brew update && brew upgrade`

puts "Updating RVM"
`rvm get head && rvm reload`

puts "Updating Vim plugins"
Dir.chdir(File.expand_path("~/src/vimbundles")) do
  `./update`
end
